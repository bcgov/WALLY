apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-minio
  labels:
    app: {{ .Release.Name }}
    component: minio
spec:
  selector:
    app: {{ .Release.Name }}
    component: minio
  ports:
    - name: minio
      port: 9000
      protocol: TCP
      targetPort: 9000